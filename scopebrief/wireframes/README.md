# Scopebrief wireframes

Open `index.html` in a browser (double-click or `python3 -m http.server` from this folder).

Click the stepper or the Continue buttons. On **screen 3**, click a left-hand supplier row or the right-hand residual line to open the fund/park/reject drawer. On Müller, use **Probable upstream** for screen 3b.
